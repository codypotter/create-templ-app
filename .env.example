PORT=8080
ASSETS_DIST_PATH=internal/assets/dist

# In dev, leave unset (defaults to /static so Go serves assets locally).
# In prod, set to your CDN base URL e.g. https://assets.com
# ASSET_BASE_URL=https://assets.com
