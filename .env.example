PORT=8080
ASSETS_DIST_PATH=internal/assets/dist

# Leave unset to serve assets locally (defaults to /static).
# Set this if assets are hosted elsewhere, e.g. a CDN base URL:
# ASSET_BASE_URL=https://assets.com
